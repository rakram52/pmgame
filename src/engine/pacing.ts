/**
 * Early-game pacing. The opening weeks are for the PM to set direction and task
 * government — not to fight fires. During the settling-in window the engine holds
 * back the pre-planted doctrine consequences and buried-secret exposures, so the
 * world RAMPS UP over the premiership instead of ambushing week 1.
 */
export const SETTLING_WEEKS = 2

/** The earliest a locked doctrine consequence may fire. */
export const EARLIEST_CONSEQUENCE_WEEK = SETTLING_WEEKS + 1
